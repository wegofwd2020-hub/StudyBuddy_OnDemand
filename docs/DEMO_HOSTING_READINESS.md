# Demo Hosting Readiness — StudyBuddy

> **Owns:** Demo-launch readiness checklist + one-page status snapshot (what's ready vs what's outstanding).
> **Adjacent in this hosting cluster:** [DEMO_LAUNCH_PLAN](DEMO_LAUNCH_PLAN.md) (canonical runbook this snapshots) · [DEMO_HOSTING_GUIDE](../../studybuddy-docs/docs/dev/DEMO_HOSTING_GUIDE.md) (architecture) · [`DOCS_INDEX.md` § Hosting](../../mambakkam-net/DOCS_INDEX.md#1-hosting-deployment--cost) (full 8-doc map).

**Generated:** 2026-05-13
**Companion docs:** [`DEMO_LAUNCH_PLAN.md`](DEMO_LAUNCH_PLAN.md) (the canonical runbook) · [`DEMO_EMAIL_INVENTORY.md`](DEMO_EMAIL_INVENTORY.md) (which mailboxes to create vs. reuse)
**Purpose:** One-page status snapshot — what's ready, the steps to take, what will be live, and what stays open after the demo is up.

---

## TL;DR

**Yes, the scripts are ready.** All 6 automation deliverables in `scripts/demo/` + `.github/workflows/deploy-demo.yml` + `docker-compose.demo.yml` are committed. The launch is **paused** (memory `project_demo_launch_pause.md`; target was Sun 2026-05-17) and resumes per `docs/DEMO_LAUNCH_PLAN.md`. The remaining work is mostly **operator/manual** — DNS, accounts, secrets, content sync, and the dry-run — not code.

---

## What's Already In the Repo (ready to use)

| Artefact | Path | Purpose |
|---|---|---|
| Demo compose override | `docker-compose.demo.yml` | Production-shaped stack; drops PgBouncer/Beat-standby/stripe-cli; adds nginx, persistent volumes, `restart: always`, json-log cap 10MB×5 |
| Provision script (2nd-tenant variant) | `scripts/demo/provision.sh` | Idempotent — clone repo to `/opt/studybuddy`, `.env.demo` skeleton, host-nginx vhost, restic backup repo, 02:00 UTC cron |
| Seed orchestrator | `scripts/demo/seed.sh` | Runs `seed_super_admin` → `seed_demo_milfordwaterford` → `seed_demo_test_account` → `seed_phase_a_dev` → `seed_dev_content` (all 5 backend scripts present) |
| Smoke test | `scripts/demo/smoke.sh` | 9 curl checks: `/healthz`, `/readyz`, 3 logins, lesson, quiz, `/`, `/demo` |
| Backup (restic) | `scripts/demo/backup.sh` + `backup-check.sh` | Daily 02:00 UTC `pg_dump` + content snapshot; weekly integrity check |
| Auto-deploy CI | `.github/workflows/deploy-demo.yml` | Build → GHCR push → SSH → `compose pull && up -d` → smoke |
| Launch runbook | `docs/DEMO_LAUNCH_PLAN.md` (955 lines, 10 sections) | Day-by-day timeline with pass/fail gates |
| Demo features (in-app) | recent commits `f924286`, `d9d4858`, `2cd82ba`, `b9bec5b` | Demo-mode flag, request-access flow, demo videos |

---

## Steps to Take for Demo Hosting

> Position: StudyBuddy is the **second tenant** on a shared Hetzner CX23 — `mambakkam.net` cuts over first.

### Pre-Day-0 — Account + DNS + Email (manual, ~3.5 hrs the night before)

1. **Domain** — register `usestudybuddy.com` at Cloudflare Registrar
2. **Cloudflare DNS** — A record `demo` → VPS IP (proxied), TTL=300s, SSL Full(strict), Origin Cert generated by mambakkam with `demo.usestudybuddy.com` in SAN
3. **Zoho Mail** — verify domain (TXT), mailboxes `support@` + `sales@`, MX/SPF/DKIM/DMARC, Gmail send-as via App Password
4. **Auth0** — free dev tenant; Student SPA + Teacher SPA + Backend M2M apps; save 5 IDs + secret + JWKS URL
5. **Stripe test mode** — `sk_test_` / `pk_test_` / webhook signing secret
6. **Sentry** — project + DSN with `environment=demo` tag

### Day 0 cutover — ~20 min cold-start (operator-driven)

1. Verify mambakkam first-tenant artefacts on shared box (Docker, UFW, fail2ban, deploy user, host nginx, `/etc/ssl/cloudflare/`)
2. `curl -fsSL .../scripts/demo/provision.sh | bash` — second-tenant variant
3. Edit `/opt/studybuddy/.env.demo` — replace 5 `<REPLACE_WITH_openssl_rand_hex_32>` placeholders + paste real Auth0/Stripe/SMTP/Sentry values
4. Append GH Actions deploy SSH pubkey to `/home/deploy/.ssh/authorized_keys` (append — don't overwrite mambakkam's)
5. `bash scripts/demo/sync-content.sh deploy@<vps>` — inject G11 visuals + rsync `content_store_data/` → `/data/content/` + rsync `web/public/sample-visuals/` → `/data/sample-visuals/` (the 44 tutorial MP4s, gitignored, never in the Docker image)
6. `docker compose -f docker-compose.yml -f docker-compose.demo.yml --env-file .env.demo pull && up -d`
7. `sudo nginx -t && sudo systemctl reload nginx` — host nginx picks up StudyBuddy vhost (reload **after** compose so the 127.0.0.1:8443 upstream is reachable)
8. `docker compose exec api alembic upgrade head && bash /app/scripts/demo/seed.sh`
9. `bash scripts/demo/smoke.sh http://127.0.0.1:8443` (loopback first)
10. Cloudflare DNS: update A record to real VPS IP, enable proxy
11. `bash scripts/demo/smoke.sh https://demo.usestudybuddy.com` — public smoke
12. 5-persona walkthrough → **GO LIVE**

### Ongoing — auto on every merge to `main`

GH Actions → build images → push GHCR → SSH `git pull && compose pull && up -d` → smoke → green or auto-file `incident:demo` issue. **No auto-rollback** (operator triages).

---

## What Will Be Available at Launch

- **`https://demo.usestudybuddy.com`** — full app behind Cloudflare proxy with Origin Cert
- **5 demo personas** working end-to-end: Super Admin, Sam Houston (school admin), Warren Buffett (teacher), Anya Iyer (G11 Commerce student), public "Try-it" G8 student, Phase A local-auth admin
- **Pre-built content**: G8/G10/G11/G12 STEM (en) + G11/G12 Commerce + G11/G12 Science (en) — ~$215 one-time Anthropic spend
- **All three auth tracks**: Auth0 student/teacher, local Phase A (school-provisioned), admin bcrypt
- **Stripe test mode** — `4242 4242 4242 4242` activates a subscription end-to-end
- **Daily encrypted backups** (restic) — 7d/4w/3m/1y retention, local-only repo at `/opt/studybuddy/backups/restic/`
- **Auto-deploy on merge** — ~5 min from `git push` to live
- **Observability** — `/metrics` scraped by CX23-local Prometheus → Grafana Cloud free tier (10k series, 13mo retention); Promtail → Loki for logs
- **Alerts** — 5 StudyBuddy-specific + 3 backup alerts → single Gmail with `[PAGE]`/`[WARN]` prefix to `siva@mambakkam.net`

---

## Work Remaining Post-Demo Hosting

### Must-do soon

| Item | Source | Priority |
|---|---|---|
| Cloudflare Access policy on `/metrics` URL (token with `RulesWriter` scope) | §7 / §10 outstanding | Med — loopback scrape works without it |
| One synthetic alert test-fire end-to-end | §10 outstanding | Med |
| Off-box backup (S3/B2/R2) — currently local-disk only | §9 BACKUPS.md residual-risk note | Med — deferred until first paying customer |
| Resume Issue **#188** — e2e school-admin → pipeline → student-visible content | CLAUDE.md tracked issues | Low — manual e2e exists, automation pending |
| Resolve Issue **#189** — re-enable `color-contrast` / `html-has-lang` / `document-title` axe rules in persona Playwright specs | CLAUDE.md tracked issues | Low |

### Polish (nice-to-have, skipped pre-launch)

- Custom branded 404 + 500 pages
- `demo.usestudybuddy.com` favicon + Apple touch icon
- Persistent "This is a demo — content resets nightly" banner
- Status page on Cloudflare Workers
- Demo data reset cadence — decide nightly vs. weekly vs. never (seed is idempotent so nightly cron is feasible)

### Bigger epics still in backlog (independent of demo hosting)

- **Epic 2** — Production launch (this demo is the prerequisite)
- **Epic 3** — Student mobile app (Expo/RN, parked behind hosting)
- **Epic 4/5** — Parent portal / District admin
- **Epic 6** — Platform hardening (K-4/K-5 need staging)
- **Epic 10** L-6 TTL sweeper (paused), L-9 per-jurisdiction read-audit, L-10 TTL override endpoint
- **Epic 11** C-5 regen continues (G11 Science in flight; G12 + Maths-heavy units pending), C-7 PDF smoke, C-8 mobile renderer parity
- **Epic 15** BR-DOC-1/BR-DOC-2 — Sys Admin + School Admin guides

### Decisions still open

- Stripe live mode toggle policy if a paying-customer demo is planned
- Sentry project — reuse prod with `demo` tag (recommended) vs. separate project
- GitHub tier upgrade triggers — stay on Free until first paying customer

---

## Risk Register (top 3)

| Risk | Likelihood | Mitigation |
|---|---|---|
| Hetzner CX23 OOM (4 GB RAM tight w/ 7 containers + Postgres + co-tenant) | Med | `restart: always`; `docker stats`; upgrade to CX33 8 GB (+$3/mo: $4.99 → $7.99) if needed |
| Real Commerce/Science lesson 500s (resolver #297) | Med | Smoke after every deploy; #297 already merged in this branch |
| Pipeline-built content metadata drift vs. seeded curriculum | Med | Verify on Day -2 before final upload |

---

**Source documents:** `docs/DEMO_LAUNCH_PLAN.md` (primary), `scripts/demo/*` (executable), `docker-compose.demo.yml`, `.github/workflows/deploy-demo.yml`. Companion docs live in `studybuddy-docs/docs/dev/DEMO_HOSTING_GUIDE.md` and `DEMO_WALKTHROUGH.md`.
